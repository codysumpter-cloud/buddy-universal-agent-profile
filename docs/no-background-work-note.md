# No Background Work Note

This branch only documents BUAP behavior. It does not add a background worker, runtime daemon, or autonomous process.

A host app must explicitly provide the runtime, permissions, and capabilities for Siri/App Intents usage.
